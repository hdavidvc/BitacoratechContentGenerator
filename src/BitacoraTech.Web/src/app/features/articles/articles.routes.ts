import { Routes } from '@angular/router';
import { ArticlePreviewComponent } from './article-preview.component';

export const articlesRoutes: Routes = [{ path: '', component: ArticlePreviewComponent }];
